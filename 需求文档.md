产品需求文档 (PRD) —— AI 提示词（Prompt）瀑布流管理网站
1. 项目概述
本项目的目标是开发一个集“收集、展示、管理”于一体的 AI 提示词（Prompt）内容聚合网站。用户可以通过该网站管理自己收集的提示词，并以高视觉吸引力的瀑布流形式进行前端展示。

2. 核心功能与页面布局
网站整体采用响应式设计，核心页面为主页（看板页）与后台管理（新增/编辑）。

2.1 主页前端布局 
顶部栏 (Header)：

搜索框： 支持对提示词标题、内容、标签的全局模糊搜索。

分类过滤器 (Tabs/Tags)： 支持按提示词“类型”或自定义“标签”进行一键筛选。

总数统计： 动态显示当前提示词库的总数量（如右上方“总计: 8040”）。

主体内容区 (Main Content)：

采用 瀑布流 (Masonry Layout) 布局。

卡片高度随内容自适应，错落有致排列。

2.2 提示词卡片 (Prompt Card) 包含字段
每个瀑布流卡片应包含且不限于以下信息：

预览图 (Preview Image)： 提示词生成的图片/效果效果图（置于卡片顶部）。

类型 (Type)： 提示词的类目（例如：产品爆炸图、手绘地图、UI样机、动漫插画等）。

生成工具 (AI Tool)： 标明该提示词适用的工具（例如：Midjourney, Stable Diffusion, GPT image 2, Nano Banana Pro,Nano Banana 2,seedream(豆包) 等）。

中文提示词 (Chinese Prompt)： 提示词的中文翻译或核心意图描述。

英文提示词 (English Prompt)： 实际输入给 AI 的英文提示词主体。

自定义标签 (Tags)： 用户自己打上的个性化标签（如：复古、3D渲染、赛博朋克），支持点击标签进行同类筛选。

快捷操作： 卡片底部提供“一键复制提示词”按钮。

2.3 管理端功能 (便捷的增删改查)
新增提示词 (Create)： 提供表单，支持上传预览图，输入中英文提示词，选择工具、类型并绑定标签。

删除提示词 (Delete)： 在卡片管理权限下或后台列表中，可一键删除。

编辑提示词 (Update)： 支持对已有提示词的所有字段进行二次修改。

3. 技术栈建议 (供 Claude/Codex 快速生成代码)
前端 (Frontend)： * 框架：React.js / Vue.js / Next.js

样式/布局：Tailwind CSS (利用其优秀的卡片与网格系统) + react-masonry-css 或 vue-masonry (实现原生的瀑布流效果)。

后端/数据库 (Backend/Database)：

轻量化方案（推荐自用）：Supabase / Firebase / Node.js + SQLite / Strapi (CMS)

需要存储预览图片（建议支持本地上传或配置 Web URL）。